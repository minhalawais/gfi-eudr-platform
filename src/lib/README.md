# Frontend Lib

This directory is reserved for:

- typed API client wrappers for `/api/v1`
- query serialization and response parsing helpers
- client-safe utility functions
- domain view-model mappers that do not contain business authority
