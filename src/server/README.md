# Frontend Server

This directory is reserved for server-only helpers such as:

- session access
- secure backend fetch wrappers
- cookie and header utilities
- server actions only when they are justified and remain thin
