# Components

Shared UI and domain components live here.

- `ui/` for reusable primitives
- `layout/` for shells, navigation, and workspace framing
- `suppliers/`, `products/`, `traceability/`, `risk/`, and similar folders for domain-specific views
- `evidence/` and `maps/` for cross-cutting compliance review surfaces
