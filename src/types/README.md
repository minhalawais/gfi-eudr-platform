# Frontend Types

Frontend-safe types live here.

- request filter types
- UI status enums mirrored from backend contracts
- DTO and view-model types derived from `/api/v1`

Do not place backend-only ORM or secret-bearing types here.
